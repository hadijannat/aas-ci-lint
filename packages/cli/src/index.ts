/**
 * @aas-ci-lint/cli
 * 
 * Command-line interface for AAS CI Linter.
 * 
 * This package provides the `aas-ci-lint` command for validating AAS files
 * from the terminal.
 * 
 * @example
 * ```bash
 * # Validate all AASX files in current directory
 * npx aas-ci-lint
 * 
 * # Validate specific files with SARIF output
 * npx aas-ci-lint --sarif results.sarif models/*.aasx
 * ```
 */

// CLI is invoked directly, no programmatic exports needed
export { };
